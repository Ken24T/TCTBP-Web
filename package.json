{
  "name": "tctbp-web",
  "version": "0.3.0",
  "private": true,
  "description": "Canonical template repository for web/browser-based projects using the TCTBP workflow",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  }
}
