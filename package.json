{
  "name": "tctbp-web",
  "version": "0.1.1",
  "private": true,
  "description": "Canonical template repository for web/browser-based projects using the TCTBP workflow",
  "scripts": {
    "test": "node -e \"console.log('TCTBP-Web template repo: no project-level tests.')\""
  }
}
