import { describe, it, expect } from 'vitest';

describe('project scaffold', () => {
  it('has a working test runner', () => {
    expect(true).toBe(true);
  });

  it('supports standard assertions', () => {
    expect(1 + 1).toBe(2);
    expect('hello').toHaveLength(5);
    expect([1, 2, 3]).toContain(2);
  });
});
